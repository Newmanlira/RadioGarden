# 🌎 Rádio Garden Mundial (via Radio Browser)

Um app interativo feito em Streamlit que permite explorar rádios do mundo todo em um mapa, usando dados reais da API pública do Radio Browser.

## 🚀 Como executar localmente

```bash
pip install -r requirements.txt
streamlit run appradio_browser_dashboard.py
